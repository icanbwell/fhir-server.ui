// import {logInfo} from '../operations/common/logging';

/**
 * Handles alert
 * @returns {Promise<void>}
 */
export const handleAlert = async (): Promise<void> => {
    console.log('Test Message from FHIR Server', {source: 'handleAlert'});
};
